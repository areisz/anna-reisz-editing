document.addEventListener('DOMContentLoaded', () => {

    // document.querySelector('.fadein').appear((element) => {
    //     const delay = element.data('delay');
    //     setTimeout(() => {
    //         element.addClass('appear');
    //     }, delay);
    // });

});
